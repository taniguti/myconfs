#if [ -S /var/run/multipass_socket ]; then
#    mygid="$(id -g)"
#    if [ "$mygid" != "$(ls -ln /var/run/multipass_socket | awk '{print $4}')" ]; then
#        sudo chgrp "$mygid" /var/run/multipass_socket
#    fi
#fi
