#
# BASH INPUTRC
# 

# Make sure check off "escape non-ascii charactors" of
# inspector of Terminal.app.
# Use option -v for ls(1).
set convert-meta off
set meta-flag on
set output-meta on

set show-all-if-ambiguous on
# Tab: menu-complete
